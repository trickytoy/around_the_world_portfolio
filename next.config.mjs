/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      ignoreDuringBuilds: true, // Ignores ESLint errors and warnings during build
    },
  };
  
  export default nextConfig;
  